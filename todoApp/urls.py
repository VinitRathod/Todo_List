from django.urls import path
from . import views

app_name = 'todoApp'

"""urlpatterns = [
    path('',views.IndexView.as_view(),name = 'index'),
    path('',views.addtoview, name = 'addtoview'),
]"""